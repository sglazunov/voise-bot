# Конспект проекта (для Claude) — linux-voise

Рабочая память по проекту: читается автоматически в начале сессии, чтобы не
изучать репозиторий заново. **Поддерживать в актуальном состоянии** — после
заметных изменений дописывать факты и решения кратко, без воды.

## Что это
**linux-voise** — сервис распознавания речи + сборки протоколов встреч
(Яндекс Телемост) под **Linux**, разворачивается через **Docker Compose**.
Приватный: весь сайт закрыт логином, данные и токены каждой команды изолированы
и зашифрованы. Владелец — Сергей (`sglazunov`), общение по-русски.
Заказчик фич внутри компании — в т.ч. директор (важна понятность протоколов).

- Рабочая копия: **`D:\linux-voise`**
- Репозиторий: **Яндекс SourceCraft**,
  `ssh://git@ssh.sourcecraft.dev/organization-katserglazunov/linux-voise.git`
  (SSH-ключ `~/.ssh/id_ed25519_sourcecraft`, без пароля; настроен `~/.ssh/config`).
- Ветка по умолчанию — `main`.

> ⚠️ **Не путать со старым репозиторием** `C:\Users\user\voice-transcriber`
> (GitHub `voice-transcriber-glazunov`). Это ДРУГАЯ, устаревшая линия развития:
> общей истории нет, Windows-ориентированная, без Docker/аутентификации/React.
> Ничего оттуда сюда не переносить и не пушить. Актуален только linux-voise.

## Запуск и разработка
```bash
cp .env.example .env                     # + свой VTX_SECRET_KEY на сервере!
docker compose up -d --build             # локально и на сервере одинаково
docker compose logs -f app
```
Сайт на `http://localhost:8000` (или `http://<IP>:8000`) → страница входа.
**Первый зарегистрированный аккаунт = основатель сервера** (ему доступны
серверные операции). Дальше — модель команд: регистрация без кода создаёт свою
команду, с кодом приглашения админа — присоединяет к его команде.

Состояние (загрузки, результаты, записи, настройки, аккаунты) — в `./data` на
хосте, переживает пересоздание контейнера. **Это папка для бэкапа.**

### Тесты
```bash
python -m pytest tests/ -q
```
`tests/conftest.py` ПРИНУДИТЕЛЬНО подменяет `VTX_DATA_DIR` на временную папку и
гасит рекордер/Ollama — иначе прогон внутри контейнера затёр бы боевые данные.
Наборы: auth/security (~40 кейсов), weeek-время, grounding, chat-stopword,
crash-recovery, key-rotation, stage7, transcribe-quality, analyze-pipeline.

### Стиль коммитов — ВАЖНО
Сообщения **на русском**, без англоязычных префиксов вида `fix(...)`.
Заголовок — суть изменения человеческим языком, часто «Что стало: почему/деталь»:
- `Стоп-слово в чате снова срабатывает: матчинг устойчив к вёрстке Телемоста`
- `Один Телемост-линк — один бот: двойной заход на встречу закрыт`
- слияния: `Слияние: авто-retry прерванного распознавания`

В теле — «Симптом / Причина / Что сделано», подробно. Трейлер
`Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>` используется.
Пользователь работает и в других сессиях → **всегда `git pull` перед работой**,
`git status` перед `git add`.

## Карта кода
```
app/main.py            FastAPI: API + отдача React-сборки; app/templates/login.html —
                       единственный серверный шаблон (остальной UI — SPA)
app/security.py        ГРАНИЦА ДОВЕРИЯ: логин/пароль (PBKDF2), сессии-куки,
                       изоляция данных по пользователю
app/user_creds.py      ключи LLM per-login; у провайдера может быть НЕСКОЛЬКО
                       ключей — ротация при упоре в лимит, чтобы не рвать протокол
app/db.py              persistence-слой: при DATABASE_URL всё, что лежало в JSON
                       под DATA_DIR, уходит в Postgres. Медиа всегда на диске
app/config.py          env-конфиг, провайдеры и тарифы моделей
app/jobs.py            очередь задач, пауза/отмена, ретеншн
app/transcribe.py      faster-whisper
app/analyze.py (1147)  ПРОТОКОЛ: схемы Pydantic, map/merge/reduce, дедуп,
                       grounding-проверка, пресеты, регенерация темы, вопрос по встрече
app/ai_context.py      постоянная база знаний команды (проекты, роли, термины),
                       подмешивается в КАЖДУЮ генерацию протокола
app/speaker_id.py      «кто говорил» из КАРТИНКИ Телемоста: активный спикер = плитка
                       с зелёной рамкой, имя читается с плитки (надёжнее, чем из текста)
app/docx_export.py     Word-протокол        app/stats.py   бизнес-метрики (переживают ретеншн)
app/notify.py sms.py   уведомления (SMS.ru и др.)
app/autosetup.py       фоновая доустановка опциональных компонентов при старте
app/diarize.py screen_ocr.py glossary.py formats.py ollama_setup.py deps_setup.py
app/automation/
  scheduler.py (1169)  демон: опрос Weeek → слоты → запись → облако → распознавание
                       → протокол → запись результата обратно в Weeek. Снапшоты
                       состояния, дедуп слотов, поздняя дозагрузка, retry
  weeek.py             клиент Weeek        settings.py  настройки/токены (per-user)
  clouds/              local | yandex_disk | gdrive
  recorder/            browser.py (Playwright/Chromium), capture.py (ffmpeg)
frontend/              React + Vite + Tailwind (App.tsx, pages/, components/, lib/)
docker/                entrypoint.sh, run.sh (Xvfb + PulseAudio + приложение)
tests/                 pytest; tests/eval — эталонные метрики качества протокола
docs/                  РУКОВОДСТВО, АРХИТЕКТУРА, БЕКЕНД, БЕЗОПАСНОСТЬ, ФУНКЦИОНАЛ,
                       СИСТЕМНЫЙ-АНАЛИЗ, БИЗНЕС-АНАЛИЗ, ПОЛЬЗОВАТЕЛЬСКИЕ-СЦЕНАРИИ,
                       ДИАГРАММЫ-UML-BPMN, ОБЗОР-ДЛЯ-ВСЕХ, ШПАРГАЛКА
```

## Протокол (app/analyze.py) — как устроен
- Схема ответа валидируется **Pydantic**: `Protocol{participants[{name,role}],
  summary, detailed[{topic,details}], key_thoughts[], conclusions[], decisions[],
  done_tasks[{task,owner}], tasks[…], minor_tasks[…]}`. Здесь **спикеры и
  ответственные ЕСТЬ и это осознанно** (источник — `speaker_id` из картинки).
- Длинные встречи: map → merge → reduce. `_MAX_CHARS=16000`, `_CHUNK_CHARS=10000`,
  overlap 800, до 16 чанков; `_dedup_maps` (окно 120 с, порог 0.85) чистит повторы;
  `_ctx_budget`/`_fit_max_tokens` подгоняют запрос под контекст движка.
- **Grounding**: `verify_protocol` требует от модели цитату-подтверждение к каждому
  пункту (`tasks/minor_tasks/done_tasks/decisions`), сверяет по стеммингу
  (`_STEM_LEN=5`) — неподтверждённое помечается как неуверенное.
- **Пресеты** (`preset_rules`/`preset_for_title`) — разные типы встреч.
- `regen_topic_details` — перегенерация одной темы; `ask_meeting` — вопрос по встрече.
- `ai_context` подмешивается всегда (иначе ИИ путает роли/проекты — жалоба директора).

### ⚠️ Открытая задача: «протокол стал совсем непонятным»
Обратная связь от людей (2026-07-27). Прошлые «этапы 1–7 качества протоколов»
уже влиты в `main` (ветка `protocol-quality` смержена, отдельной работы там нет).
Правила работы над этим:
1. **Сначала посмотреть реальный плохой протокол**, а не править промпт вслепую.
2. **Мерять, а не «на глаз»**: `tests/eval/run_eval.py` + кейсы в `tests/eval/cases/`
   — эталонная метрика, гоняется при любой правке промпта/смене модели.
3. Спикеров и ответственных НЕ выпиливать: в этом проекте они обоснованы.
   Скорее всего дело в структуре/формулировках: дробные темы, канцелярит,
   дубли между разделами, потеря сути на reduce.

## Инфраструктура
- **Docker**: три сервиса — `app` (образ `linux-voise:latest`), `db`
  (postgres:16-alpine), `cloudflared` (туннель); все `restart: unless-stopped`,
  профилей в compose нет. Сборка двухстадийная: Node собирает SPA →
  Python-образ `python:3.12-slim-bookworm`.
  (Мелочь: README в «Решении проблем» упоминает `--profile local-ai` — такого
  профиля в compose нет, текст устарел.)
- Пин на **bookworm** осознанный: в trixie загрузчик отвергает ctranslate2 4.4.0
  («cannot enable executable stack»).
- **Рекордер на Linux работает**: `docker/run.sh` поднимает **Xvfb** (экран `:99`,
  `VTX_SCREEN_RES`) и **PulseAudio** с null-sink `meet0…meetN` — по одному на слот
  записи; ffmpeg пишет `meetN.monitor` + x11grab. Бот заходит с выключенными
  микрофоном и камерой. Отключается `VTX_RECORDER_ENABLED=0`.
- **Ollama НЕ ставится в контейнер** — подключается снаружи по `OLLAMA_URL`
  (по умолчанию хост). Облачные ключи — через `.env` или UI.
- Ключевые env: `VTX_SECRET_KEY` (шифрование секретов — задать до первого
  сохранения токенов!), `VTX_MODEL`, `VTX_RECORDER_ENABLED`, `VTX_HTTPS`,
  `VTX_REGISTRATION_CODE`, `TZ`. Полная таблица — в README.
- Обновление: `git pull && docker compose up -d --build`.

## Особенности, которые легко нарушить
- **Регистрация открыта всем**, кто знает адрес (создаётся отдельная пустая
  команда). Ограничение — только на уровне прокси/VPN; переключателя в приложении
  нет. Не заявлять пользователю, что сервис «закрыт», без этой оговорки.
- Секреты шифруются ключом, производным от `VTX_SECRET_KEY`; при его потере
  сохранённые токены не расшифровать.
- Ретеншн задач — 24 ч; бизнес-метрики специально пишутся в `stats.py`, чтобы
  пережить очистку. Не «чинить» это как дублирование.
- Вёрстка Телемоста нестабильна: селекторы в `recorder/browser.py` — списки
  кандидатов; при поломке есть скриншоты и логи, «Тест записи» и «Проверить звук».
- Есть остановка записи **стоп-словом в чате** встречи (`VTX_CHAT_STOP_MAX_TOKENS`).
