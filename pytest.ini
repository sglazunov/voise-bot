[pytest]
# Без этого «pytest -q» (команда из документации) падает
# ModuleNotFoundError: app — корень проекта не попадает в sys.path.
# «python -m pytest» работал случайно: он сам добавляет текущий каталог.
pythonpath = .
testpaths = tests
addopts = --ignore=tests/eval
