"""Meeting automation: Weeek → record Telemost → cloud → transcribe → protocol.

This package is independent of the core transcription app — it orchestrates the
existing pipeline (app.jobs) around live meetings. See docs/automation-plan.md.
"""
